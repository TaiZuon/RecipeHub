spring:
  application:
    name: ingredient_service
  datasource:
    url: jdbc:mysql://localhost:3306/ingredient_service
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
server:
  port: 8081